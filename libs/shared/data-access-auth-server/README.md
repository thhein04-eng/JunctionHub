# data-access-auth-server

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build data-access-auth-server` to build the library.

## Running unit tests

Run `nx test data-access-auth-server` to execute the unit tests via [Jest](https://jestjs.io).
